import { BluefontDirective } from './bluefont.directive';

describe('BluefontDirective', () => {
  it('should create an instance', () => {
    const directive = new BluefontDirective();
    expect(directive).toBeTruthy();
  });
});
