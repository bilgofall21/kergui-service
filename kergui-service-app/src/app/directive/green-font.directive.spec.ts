import { GreenFontDirective } from './green-font.directive';

describe('GreenFontDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenFontDirective();
    expect(directive).toBeTruthy();
  });
});
